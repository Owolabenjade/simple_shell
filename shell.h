#ifndef SHELL_H
#define SHELL_H

/* Standard Library Includes for input/output, memory allocation, and process control */
#include <stdio.h>  /* For printf and getline functions */
#include <stdlib.h> /* For exit, malloc, and free functions */
#include <unistd.h> /* For fork, getpid, execve functions */
#include <string.h> /* For string manipulation functions, if needed */
#include <sys/types.h> /* For pid_t type */
#include <sys/wait.h> /* For wait function */

/* Add any custom function prototypes here.
   For the current implementation in simple_shell.c, we don't have custom functions.
   However, if you expand your shell, you might add function prototypes like:
void my_function(void);
*/

int main(void)

#endif /* SHELL_H */
